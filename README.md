# techcreate_repo
Repo for tech create interview

# Fixed Length Parser
A dynamic fixed-length file parser using a schema without changing code.

## Features
- Support dynamic txt schema for parsing.
- Validations for short lines and blank values.
- JUnit to test dynamic schema, validations and large files parsing.
